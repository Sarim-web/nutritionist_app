name: nutritionist_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1          # or latest stable
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fl_chart: ^0.68.0                 # check pub.dev for latest
  connectivity_plus: ^6.0.0
  permission_handler: ^11.0.0
  intl: ^0.19.0
  go_router: ^14.0.0                # or latest

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
